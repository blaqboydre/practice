#include <stdio.h>

int main()
{
	    int x;
	        printf("Enter a number: ");
		    scanf("%d", &x);
		        
		        if (x > 17)
				    printf("Person is eligible to vote\n");
			    
			    else
				        printf("Person is underage\n");
			        return 0;
}
